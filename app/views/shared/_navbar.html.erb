<nav class="bg-lime-300">
  <div class="container mx-auto flex justify-between items-center p-4">
    <span class="text-xl font-bold text-blue-400">
      <%= link_to "Home", root_path, class: "hover:text-blue-300" %>
    </span>
    <div class="flex items-center space-x-4">
      <% if authenticated? %>
        <%= button_to "Log out", session_path, method: :delete, class: "bg-blue-600 hover:bg-blue-500 text-white font-medium py-2 px-4 rounded" %>
      <% else %>
        <%= link_to "Log in", new_session_path, class: "bg-blue-600 hover:bg-blue-500 text-white font-medium py-2 px-4 rounded" %>
      <% end %>
      <!-- somewhere in the navbar or profile -->

    </div>
  </div>
</nav>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow">
  <div class="container-fluid">
    <!-- Logo or Home Link -->
    <a class="navbar-brand fw-bold" href="<%= root_path %>">HR Dashboard</a>

    <!-- Toggle Button for Mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Links -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if Current.user&.admin? %>
          <!-- Admin Links -->
          <li class="nav-item">
            <%= link_to "Admin Dashboard", admin_dashboard_path, class: "nav-link fw-bold" %>
          </li>
          <li class="nav-item">
            <%= link_to "Assign Task", admin_assign_task_path, class: "nav-link fw-bold" %>
          </li>
        <% end %>

        <% if Current.user %>
          <!-- Log Out Button -->
          <li class="nav-item">
            <%= button_to "Log out", session_path, method: :delete, class: "btn btn-danger btn-sm ms-3 fw-bold" %>
          </li>
        <% else %>
          <!-- Log In Link -->
          <li class="nav-item">
            <%= link_to "Log in", new_session_path, class: "btn btn-success btn-sm ms-3 fw-bold" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>